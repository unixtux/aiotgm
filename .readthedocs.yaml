version: 1.2.6

build:
  os: ubuntu-22.04
  tools:
    python: ">=3.8"

sphinx:
  builder: html
  configuration: docs/conf.py
  fail_on_warning: true

python:
    install:
        - requirements: requirements.txt
        - method: pip
            path: .
